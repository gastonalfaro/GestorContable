package TESTING;
import SIGNER.*;
import SMART_CARD.*;

public class Test {
    public static void main(String[] args) {
        
        //DriverXML driver_xml = new DriverXML("C:\\Users\\ramses\\Desktop\\Ramses.xml");
        /*DigitalSigner signer = new DigitalSigner();
        Certificado certificado_ = signer.get_certificado_valido("1224");
        if (certificado_.getLlave() != null) {
            System.out.println("Llave extraida del certificado !!!");
            System.out.println("LLAVE >> " + certificado_.getLlave().getEncoded());
        } else {
            System.out.println("ERROR >> " + signer.getERROR_DIGITAL_SIGNER());
        }*/
        /*if (certificado_ != null) {
            Emisor emisor_ = signer.get_emisor(certificado_.getCertificado_x509());
            Sujeto sujeto_ = signer.get_sujeto(certificado_.getCertificado_x509());
            if (driver_xml.set_info_certificado(emisor_, sujeto_)) {
                System.out.println("Se almaceno la información del emisor en documento xml !!!");
            } else {
                System.out.println("ERROR >> " + driver_xml.getERROR_DRIVER_XML());
            }
        } else {
            System.out.println("ERROR >> " + signer.getERROR_DIGITAL_SIGNER());
        }*/
    }//main
}//CLASE
